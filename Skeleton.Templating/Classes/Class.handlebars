// generated by a tool
using System;
using System.ComponentModel;
using System.ComponentModel.DataAnnotations;

namespace {{cs_name Namespace}}.Data.Domain 
{
	public class {{cs_name Name}} 
	{
        public {{cs_name Name}}({{#each NonExcludedFields}}{{format_clr_type ClrType}} {{cml_case Name}}{{#unless @last}}, {{/unless}}{{/each}})
        {
            {{#each NonExcludedFields}}
            {{cs_name Name}} = {{cml_case Name}};
            {{/each}}    
        }

        {{#each NonExcludedFields}}
		[DisplayName("{{hmn}}")]{{#if HasSize}}[MaxLength({{Size}})]{{/if}}{{#if IsRequired}}[Required]{{/if}}
		public {{format_clr_type ClrType}} {{cs_name Name}} {get; set;}

		{{/each}}
	}

}