-- generated by a tool

insert into {{{escape_sql Name}}} (
{{#each TestData}}
    {{{escape_sql Field.Name}}}{{#unless @last}},{{/unless}}
{{/each}}
)
VALUES
(
{{#each TestData}}
    {{{Value}}}{{#unless @last}},{{/unless}}
{{/each}}
);