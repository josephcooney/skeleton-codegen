// generated by a tool
import React, { FunctionComponent } from 'react';
import {TableCell, TableRow, ButtonBase} from "@mui/material";

interface {{cs_name Name}}HeaderProps {
    onClick? : (fieldName: string) => void;
}

export const {{cs_name Name}}Header : FunctionComponent<{{cs_name Name}}HeaderProps> = (props: {{cs_name Name}}HeaderProps) => {
    return (<TableRow>
                {{#each ListFields}}
                {props.onClick ?
                    <TableCell className='table-header'><ButtonBase className='table-header-btn' onClick={_ => props.onClick ? props.onClick("{{Name}}") : null}>{{DisplayName}}</ButtonBase></TableCell>
                    : <TableCell>{{DisplayName}}</TableCell> 
                }
                {{/each}}
                {{#if UnderlyingType.IsAttachmentWithThumbnail}}
                <TableCell></TableCell>
                {{/if}}
                <TableCell></TableCell>
            </TableRow>);
}