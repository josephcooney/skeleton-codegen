// generated by a tool
import {Collapse, Divider, List, ListItemButton, ListItemText } from "@mui/material";
import ListItemLink from "./ListItemLink";
import React from "react";
import ExpandLess from '@mui/icons-material/ExpandLess';
import ExpandMore from '@mui/icons-material/ExpandMore';

export default function LeftNavItems() {

const [open, setOpen] = React.useState(false);

const handleClick = () => {
setOpen(!open);
};

return <><List>
    {{#each NonReferenceTypesThatHaveUI}}
        <ListItemLink to="/{{kb_case Name}}" primary="{{hmn Name}}" />
    {{/each}}
    <Divider/>
    <ListItemButton onClick={handleClick}>
        <ListItemText primary="Reference Data" />
        {open ? <ExpandLess /> : <ExpandMore />}
    </ListItemButton>
    <Collapse in={open} timeout="auto" unmountOnExit>
        <List component="div" disablePadding>
            {{#each ReferenceTypesThatHaveUI}}
                <ListItemLink to="/{{kb_case Name}}" primary="{{hmn Name}}" />
            {{/each}}
        </List>
    </Collapse>
</List>
</>;
}