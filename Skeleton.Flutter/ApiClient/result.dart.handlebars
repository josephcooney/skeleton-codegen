// generated by a tool
export interface {{cs_name Name}} {

{{#each Fields}}
    {{#unless IsExcludedFromResults}}
        {{cml_case Name}} : {{get_ts_type ClrType}}{{#unless IsRequired}} | null{{/unless}};
    {{/unless}}

{{/each}}
}