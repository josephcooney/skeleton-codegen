-- generated by a tool
SET search_path TO {{{escape_sql Namespace}}};

DROP TYPE IF EXISTS {{{make_db_name Name "result"}}} CASCADE;

CREATE TYPE {{{make_db_name Name "result"}}} AS 
    ({{#each Fields}} 
        {{{escape_sql Name}}} {{{escape_sql_name ProviderTypeName}}}{{#if HasSize}}({{Size}}){{/if}}{{#unless @last}},{{/unless}}{{/each}}
    );

COMMENT ON TYPE {{{make_db_name Name "result"}}} IS '{"applicationtype":"{{Name}}", "generated":true, "isResult":true}';