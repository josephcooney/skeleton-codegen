-- generated by a tool
SET search_path TO {{{escape_sql_keyword Namespace}}};

DROP TYPE IF EXISTS {{Name}}_result CASCADE;

CREATE TYPE {{Name}}_result AS 
    ({{#each Fields}} 
        {{{escape_sql_keyword Name}}} {{ProviderTypeName}}{{#if HasSize}}({{Size}}){{/if}}{{#unless @last}},{{/unless}}{{/each}}
    );

COMMENT ON TYPE {{Name}}_result IS '{"applicationtype":"{{Name}}", "generated":true, "isResult":true}';