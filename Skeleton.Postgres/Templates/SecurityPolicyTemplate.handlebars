-- generated by a tool
SET search_path TO {{{escape_sql Namespace}}};

alter table {{{escape_sql Name}}} enable row level security;

drop policy if exists {{Name}}_policy on {{{escape_sql Name}}};

drop policy if exists {{Name}}_select_policy on {{{escape_sql Name}}};

drop policy if exists {{Name}}_insert_policy on {{{escape_sql Name}}};

drop policy if exists {{Name}}_update_policy on {{{escape_sql Name}}};

drop policy if exists {{Name}}_delete_policy on {{{escape_sql Name}}};

-- select policy
create policy {{Name}}_select_policy
on {{{escape_sql Name}}}
FOR SELECT
USING
(
{{#if UnderlyingType.IsReferenceData}}
(TRUE) -- anyone can read reference data
{{else}}
	{{#if AllowAnonView}}
	(TRUE) -- anon can read {{Name}}
	{{else}}
		{{#if AllowUserReadAll}}
	(
	-- authenticated users can read {{Name}}
	current_setting('app.user_id', 't') is not null
	) 
		{{else}}
			{{#if AllowUserView}}
	(
	-- owning users can read
	{{OwnershipExpression}}
	)
	OR
			{{/if}}
	(
		-- allow admin to read
        select is_system or is_admin
        from {{{escape_sql UserType.Namespace}}}.{{{escape_sql UserType.Name}}} u
        where u.id = current_setting('app.user_id', 't')::{{UserIdField.ProviderTypeName}}
	)
		{{/if}}
	{{/if}}
{{/if}}
);

-- insert policy
create policy {{Name}}_insert_policy
on {{{escape_sql Name}}}
FOR INSERT
WITH CHECK
(
{{#if UnderlyingType.IsReferenceData}}
	(
		-- allow admin to modify reference data
        select is_system or is_admin
        from {{{escape_sql UserType.Namespace}}}.{{{escape_sql UserType.Name}}} u
        where u.id = current_setting('app.user_id', 't')::{{UserIdField.ProviderTypeName}}
	)
{{else}}
	{{#if AllowAnonAdd}}
	(TRUE) -- anon can add
	{{else}}
		{{#if AllowUserAdd}}
	(
	-- authenticated users can add
	current_setting('app.user_id', 't') is not null
	) 
		{{else}}
	(
		-- allow admin to modify data
        select is_system or is_admin
        from {{{escape_sql UserType.Namespace}}}.{{{escape_sql UserType.Name}}} u
        where u.id = current_setting('app.user_id', 't')::{{UserIdField.ProviderTypeName}}
	)


		{{/if}}
	{{/if}}
{{/if}}
);

-- update policy
create policy {{Name}}_update_policy
on {{{escape_sql Name}}}
FOR UPDATE
USING 
(
{{#if UnderlyingType.IsReferenceData}}
	(
		-- allow admin to modify reference data
        select is_system or is_admin
        from {{{escape_sql UserType.Namespace}}}.{{{escape_sql UserType.Name}}} u
        where u.id = current_setting('app.user_id', 't')::{{UserIdField.ProviderTypeName}}
	)
{{else}}
	{{#if AllowAnonEdit}}
	(TRUE) -- anon can edit
	{{else}}
		{{#if AllowUserEditAll}}
	(
	-- authenticated users can edit 
	current_setting('app.user_id', 't') is not null
	) 
		{{else}}
			{{#if AllowUserEdit}}
	-- users can edit the {{Name}} they own	
	{{OwnershipExpression}}
	OR
			{{/if}}
	(
		-- admin can update {{Name}}
        select is_system or is_admin
        from {{{escape_sql UserType.Namespace}}}.{{{escape_sql UserType.Name}}} u
        where u.id = current_setting('app.user_id', 't')::{{UserIdField.ProviderTypeName}}
	)
		{{/if}}
	{{/if}}
{{/if}}
);

{{#if HardDelete}}

-- delete policy
create policy {{Name}}_delete_policy
on {{{escape_sql Name}}}
FOR DELETE
USING
(
{{#if UnderlyingType.IsReferenceData}}
	(
		-- admin can delete {{Name}} reference data
        select is_system or is_admin
        from {{{escape_sql UserType.Namespace}}}.{{{escape_sql UserType.Name}}} u
        where u.id = current_setting('app.user_id', 't')::{{UserIdField.ProviderTypeName}}
	)
{{else}}
	{{#if AllowAnonDelete}}
	(TRUE) -- anon can delete
	{{else}}
			{{#if AllowUserDelete}}
	-- users can edit the {{Name}} they own	
	{{OwnershipExpression}}
	OR
			{{/if}}
	(
		-- admin can delete {{Name}}
        select is_system or is_admin
		from {{{escape_sql UserType.Namespace}}}.{{{escape_sql UserType.Name}}} u
		where u.id = current_setting('app.user_id', 't')::{{UserIdField.ProviderTypeName}}
	)
		{{/if}}
{{/if}}
);
{{/if}}

