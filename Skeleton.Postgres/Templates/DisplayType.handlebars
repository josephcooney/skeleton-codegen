-- generated by a tool

DROP TYPE IF EXISTS {{Name}}_display CASCADE;

CREATE TYPE {{Name}}_display AS 
    ({{#each DisplayAllFields}} 
        {{#if HasDisplayName}}{{{escape_sql_keyword DisplayName}}}{{else}}{{{escape_sql_keyword Name}}}{{/if}} {{ProviderTypeName}}{{#if HasSize}}({{Size}}){{/if}}{{#unless @last}},{{/unless}}{{/each}}
    );

COMMENT ON TYPE {{Name}}_display IS '{"applicationtype":"{{Name}}", "generated":true}';