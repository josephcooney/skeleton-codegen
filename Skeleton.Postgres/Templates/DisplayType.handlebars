-- generated by a tool

DROP TYPE IF EXISTS {{Namespace}}.{{{make_db_name Name "display"}}} CASCADE;

CREATE TYPE {{Namespace}}.{{{make_db_name Name "display"}}} AS 
    ({{#each DisplayAllFields}} 
        {{#if HasDisplayName}}{{{escape_sql DisplayName}}}{{else}}{{{escape_sql Name}}}{{/if}} {{{escape_sql_name ProviderTypeName}}}{{#if HasSize}}({{Size}}){{/if}}{{#unless @last}},{{/unless}}{{/each}}
    );

COMMENT ON TYPE {{Namespace}}.{{{make_db_name Name "display"}}} IS '{"applicationtype":"{{Name}}", "generated":true}';