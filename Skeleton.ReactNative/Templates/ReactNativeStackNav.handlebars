// generated by a tool
import React from 'react';
import {createNativeStackNavigator} from '@react-navigation/native-stack';

{{#each TypesThatHaveUI}}
import { {{cs_name Name}}ListScreen } from './domain/{{kb_case Name}}/list/{{cml_case Name}}ListScreen'
// TODO import detail
{{/each}}

export type AppStackParamList = {
{{#each TypesThatHaveUI}}
    {{cs_name Name}}List: undefined;
{{/each}}
    // TODO detail will look like this Address: {id: number};
};

interface StackNavProps {
    routeName: keyof AppStackParamList | undefined;
}

const Stack = createNativeStackNavigator<AppStackParamList>();

export const StackNav = (props: StackNavProps):JSX.Element => {
    return (
    <Stack.Navigator initialRouteName={props.routeName}>
        {{#each TypesThatHaveUI}}
        <Stack.Screen name="{{cs_name Name}}List" component={ {{cs_name Name}}ListScreen } />
        {{/each}}
    </Stack.Navigator>
    );
}