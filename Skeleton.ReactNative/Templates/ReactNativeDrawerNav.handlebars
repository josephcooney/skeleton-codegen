// generated by a tool
import React from 'react';
import {createDrawerNavigator} from '@react-navigation/drawer';

import {HomeScreen} from './HomeScreen';
{{#each NonReferenceTypesThatHaveUI}}
import { {{cs_name Name}}Screen } from './domain/{{kb_case Name}}/{{cml_case Name}}Screen'
{{/each}}

export type AppDrawerParamList = {
    HomeScreen: undefined;
};

const Drawer = createDrawerNavigator();

export const DrawerNav = (): JSX.Element => {

return (<Drawer.Navigator initialRouteName="home">
    <Drawer.Screen name="HomeScreen" component={HomeScreen} />
{{#each NonReferenceTypesThatHaveUI}}
    <Drawer.Screen name="{{hmn Name}}" component={ {{cs_name Name}}Screen } options={ {headerShown: false} } />
{{/each}}        
</Drawer.Navigator>);
}